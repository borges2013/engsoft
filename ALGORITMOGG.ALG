Algoritmo "algoritmoGG"

Var
//  Declarações das variáveis
   A:inteiro
   B, C, D: real

Inicio

 A <- abs(-50)
 escreval (A)
 
 B <- exp(9, 5)
 escreval (B)
 
 C <- int(raizq(90))
    escreval (C)
    
    D <- sen(GraupRad(90))
    escreva (D)


Fimalgoritmo